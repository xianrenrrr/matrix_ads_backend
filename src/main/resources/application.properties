spring.application.name=demo
server.port=${PORT:8080}
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB
firebase.enabled=true
firebase.storage.bucket=matrix_ads_video
firebase.service-account-key=/Users/liqiyang/Desktop/project/matrix_ads/matrix_ads_backend/serviceAccountKey.json
# OpenAI Configuration (GPT-4o-mini for vision)
# Priority: 1. Environment variable (production) 2. Local properties file (development) 3. Empty (fallback)
# Note: DeepSeek API doesn't support vision models, using GPT-4o-mini as cost-effective alternative
openai.api.key=${OPENAI_API_KEY:${openai.api.key.local:}}


